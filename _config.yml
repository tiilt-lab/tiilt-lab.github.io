# Minimal Jekyll config — site is mostly static HTML.
# Only files with front matter (---) are processed by Liquid.
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - scripts

# Blog post permalink structure
permalink: /blog/:year/:month/:day/:title/
